-- This migration is now a no-op because:
-- 1. value5Years already exists in the initial migration
-- 2. serviceValue already exists in the initial migration
-- 3. Default values are already set in the initial migration

-- Migration kept for history but does nothing
SELECT 1;
